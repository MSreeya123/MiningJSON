_schema-version: "3.2"
ID: appmininginfo
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: appmininginfo-destination-content
  type: com.sap.application.content
  requires:
  - name: appmininginfo-destination-service
    parameters:
      content-target: true
  - name: appmininginfo-repo-host
    parameters:
      service-key:
        name: appmininginfo-repo-host-key
  - name: appmininginfo-uaa
    parameters:
      service-key:
        name: appmininginfo-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: appmininginfo_html_repo_host
          ServiceInstanceName: appmininginfo-html5-service
          ServiceKeyName: appmininginfo-repo-host-key
          sap.cloud.service: appmininginfo
        - Authentication: OAuth2UserTokenExchange
          Name: appmininginfo_uaa
          ServiceInstanceName: appmininginfo-xsuaa-service
          ServiceKeyName: appmininginfo-uaa-key
          sap.cloud.service: appmininginfo
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: appmininginfo-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: appmininginfo-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - appmininginfo.zip
      name: appmininginfo
      target-path: resources/
- name: appmininginfo
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: appmininginfo-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: appmininginfo-destination-service
    service-plan: lite
- name: appmininginfo-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: appmininginfo-xsuaa-service
    service-plan: application
- name: appmininginfo-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: appmininginfo-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
